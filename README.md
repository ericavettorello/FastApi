# Автоматическое развертывание Docker контейнеров

Система автоматического развертывания с использованием Docker Registry и автоматического обновления контейнеров.

> 📖 **Важно:** Auto-updater **НЕ** отслеживает изменения в Docker Desktop автоматически. Он работает только с локальным registry на сервере. Подробнее о процессе развертывания см. [DEPLOYMENT.md](DEPLOYMENT.md)

## Компоненты

### 1. Docker Registry
Локальное хранилище Docker-образов на порту `5000`.

**Доступ через браузер:**
⚠️ **Важно:** Registry работает по **HTTP**, а не HTTPS. Используйте:
- `http://localhost:5000/v2/_catalog` - список репозиториев
- `http://localhost:5000/v2/` - API endpoint

Если браузер показывает ошибку SSL, убедитесь, что используете `http://` (не `https://`).

**Использование через командную строку:**

> **Где выполнять:** Команды выполняются в терминале на хосте, где установлен Docker. В Cursor можно использовать встроенный терминал (Terminal → New Terminal) или внешний терминал системы.
>
> **Важно:** Замените `your-image:tag` на реальное имя и тег вашего образа. Например, если у вас есть образ `myapp:v1.0`, команда будет:
> ```bash
> docker tag myapp:v1.0 localhost:5000/myapp:v1.0
> docker push localhost:5000/myapp:v1.0
> ```

```bash
# ============================================
# ОСНОВНЫЕ ОПЕРАЦИИ С ОБРАЗАМИ
# ============================================

# 1. Отправка образа в registry
# Сначала создайте тег для образа (замените your-image:tag на реальное имя)
docker tag your-image:tag localhost:5000/your-image:tag
# Затем отправьте образ в registry
docker push localhost:5000/your-image:tag

# Пример с реальным образом:
# docker tag nginx:latest localhost:5000/nginx:latest
# docker push localhost:5000/nginx:latest

# Перед отправкой проверьте список ваших образов:
# docker images

# 2. Загрузка образа из registry
docker pull localhost:5000/your-image:tag

# 3. Просмотр локальных образов из registry
docker images | grep localhost:5000

# Просмотр конкретного образа с форматированием
docker images localhost:5000/your-image --format "table {{.Repository}}\t{{.Tag}}\t{{.Size}}"

# 4. Удаление образа из registry (через удаление локального)
docker rmi localhost:5000/your-image:tag

# ============================================
# API ENDPOINTS (через curl)
# ============================================

# Проверка доступности registry
curl http://localhost:5000/v2/

# Получить список всех репозиториев
curl http://localhost:5000/v2/_catalog

# Получить список тегов конкретного образа
curl http://localhost:5000/v2/your-image/tags/list

# Получить информацию о манифесте образа (требует digest)
curl -I http://localhost:5000/v2/your-image/manifests/latest

# ============================================
# РАБОТА С DOCKER
# ============================================

# Просмотр информации об образе из registry
docker image inspect localhost:5000/your-image:tag

# Получить digest образа
docker image inspect localhost:5000/your-image:tag --format '{{.Id}}'

# Запустить контейнер из образа registry
docker run -d --name my-container localhost:5000/your-image:tag

# ============================================
# ПОЛЕЗНЫЕ КОМАНДЫ
# ============================================

# Проверка размера volume registry
docker volume inspect autodeploy_registry-data --format '{{.Mountpoint}}' | xargs du -sh

# Просмотр логов registry
docker logs docker-registry

# Перезапуск registry
docker restart docker-registry

# Остановка всех сервисов (из директории проекта)
cd /root/Autodeploy && docker compose down

# Запуск всех сервисов (из директории проекта)
cd /root/Autodeploy && docker compose up -d

# Пересборка и перезапуск auto-updater (из директории проекта)
cd /root/Autodeploy && docker compose up -d --build auto-updater

# Альтернатива: указать путь к файлу явно
docker compose -f /root/Autodeploy/docker-compose.yml up -d
```

### 2. Auto-Updater
Автоматическое обновление контейнеров при появлении новых версий образов в registry.

**Особенности:**
- Совместим с Docker API 1.44+
- Проверяет обновления каждые 30 секунд
- Автоматически обновляет контейнеры из локального registry
- **Автоматически обновляет системные контейнеры (registry и другие)**
- Поддерживает очистку старых образов
- Исключение контейнеров из обновления через метку `com.autodeploy.update=false`

## Запуск

⚠️ **Важно:** Команды `docker compose` нужно выполнять из директории проекта, где находится файл `docker-compose.yml`.

```bash
# Перейдите в директорию проекта
cd /root/Autodeploy

# Запустите сервисы
docker compose up -d

# Или укажите путь к файлу явно
docker compose -f /root/Autodeploy/docker-compose.yml up -d
```

## Настройка

### Переменные окружения Auto-Updater:

- `POLL_INTERVAL` - интервал проверки обновлений в секундах (по умолчанию: 30)
- `REGISTRY_URL` - URL локального registry (по умолчанию: localhost:5000)
- `CLEANUP` - удаление старых образов после обновления (по умолчанию: true)
- `TRACK_SYSTEM_CONTAINERS` - отслеживание системных контейнеров (registry, nginx и т.д.) (по умолчанию: true)
- `SYSTEM_CONTAINERS` - список системных контейнеров для отслеживания через запятую (по умолчанию: docker-registry)

### Исключение контейнеров из обновления:

Добавьте метку к контейнеру:
```yaml
labels:
  - "com.autodeploy.update=false"
```

## Просмотр логов

```bash
# Логи всех сервисов (из директории проекта)
cd /root/Autodeploy && docker compose logs -f

# Логи только auto-updater
cd /root/Autodeploy && docker compose logs -f auto-updater

# Логи только registry
cd /root/Autodeploy && docker compose logs -f registry

# Или с указанием пути к файлу
docker compose -f /root/Autodeploy/docker-compose.yml logs -f
```

## Проверка статуса

```bash
docker compose ps
```

## Просмотр информации о сервере

Полезные команды для проверки состояния Docker и сервера:

```bash
# ============================================
# DOCKER ОБРАЗЫ
# ============================================

# Список всех образов с подробной информацией
docker images

# Список образов с форматированием (компактный вид)
docker images --format "table {{.Repository}}\t{{.Tag}}\t{{.Size}}\t{{.CreatedAt}}"

# Список образов из локального registry
docker images | grep localhost:5000

# Размер всех образов
docker images --format "{{.Repository}}:{{.Tag}} - {{.Size}}" | sort

# ============================================
# КОНТЕЙНЕРЫ
# ============================================

# Только запущенные контейнеры
docker ps

# Все контейнеры (включая остановленные)
docker ps -a

# Контейнеры с форматированием
docker ps --format "table {{.Names}}\t{{.Image}}\t{{.Status}}\t{{.Ports}}"

# Статус контейнеров проекта (из директории проекта)
cd /root/Autodeploy && docker compose ps

# Или с указанием пути к файлу
docker compose -f /root/Autodeploy/docker-compose.yml ps

# ============================================
# VOLUMES (хранилища данных)
# ============================================

# Список всех volumes
docker volume ls

# Информация о конкретном volume
docker volume inspect autodeploy_registry-data

# Размер volume registry
docker volume inspect autodeploy_registry-data --format '{{.Mountpoint}}' | xargs du -sh

# ============================================
# СЕТИ
# ============================================

# Список всех сетей
docker network ls

# Информация о сети
docker network inspect autodeploy_autodeploy-network

# ============================================
# ФАЙЛЫ ПРОЕКТА
# ============================================

# Список файлов в проекте
ls -lah /root/Autodeploy/

# Содержимое docker-compose.yml
cat /root/Autodeploy/docker-compose.yml

# ============================================
# ОБЩАЯ ИНФОРМАЦИЯ
# ============================================

# Информация о Docker
docker info

# Версия Docker
docker --version
docker compose version

# Использование ресурсов
docker stats --no-stream

# Использование дискового пространства
docker system df

# Детальная информация об использовании места
docker system df -v
```

## Хранение данных

Данные registry хранятся в Docker volume `autodeploy_registry-data`:
```bash
# Просмотр информации о volume
docker volume inspect autodeploy_registry-data

# Просмотр содержимого
ls -la /var/lib/docker/volumes/autodeploy_registry-data/_data
```

## Как это работает

1. CI/CD пайплайн собирает новый образ приложения
2. Образ отправляется в локальный Docker Registry (`localhost:5000`)
3. Auto-Updater периодически проверяет registry на наличие обновлений
4. При обнаружении нового образа контейнер автоматически обновляется:
   - Старый контейнер останавливается и удаляется
   - Создается новый контейнер с обновленным образом
   - Новый контейнер запускается с сохранением всех настроек
   - Старый образ удаляется (если включена очистка)

## Преимущества

- ✅ Полная автоматизация развертывания
- ✅ Совместимость с новым Docker API
- ✅ Гибкая настройка через переменные окружения
- ✅ Логирование всех операций
- ✅ Безопасное обновление с сохранением конфигурации

