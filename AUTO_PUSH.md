# Автоматическая отправка на GitHub

## 🚀 Настройка автоматической отправки

После каждого коммита изменения автоматически отправляются на GitHub.

## ⚙️ Установка

### Вариант 1: Автоматическая установка (рекомендуется)

```bash
cd /root/Autodeploy
bash auto-push-setup.sh
```

Скрипт настроит:
- Post-commit hook для автоматического push
- Credential helper для сохранения токена
- SSH или HTTPS аутентификацию

### Вариант 2: Ручная установка

```bash
cd /root/Autodeploy

# Создать post-commit hook
cat > .git/hooks/post-commit << 'EOF'
#!/bin/bash
BRANCH=$(git rev-parse --abbrev-ref HEAD)
git push origin "$BRANCH" 2>&1
EOF

chmod +x .git/hooks/post-commit
```

## 🔐 Настройка аутентификации

### Personal Access Token (HTTPS)

1. Создайте токен: https://github.com/settings/tokens
   - Нажмите "Generate new token (classic)"
   - Выберите права: `repo`
   - Скопируйте токен

2. При первом push введите:
   - Username: `ericavettorello`
   - Password: ваш Personal Access Token

3. Git сохранит токен для будущих операций:
   ```bash
   git config --global credential.helper store
   ```

### SSH ключ

1. Создайте SSH ключ:
   ```bash
   ssh-keygen -t ed25519 -C "your_email@example.com"
   ```

2. Добавьте ключ в GitHub:
   - Скопируйте публичный ключ: `cat ~/.ssh/id_ed25519.pub`
   - Добавьте: https://github.com/settings/keys

3. Измените remote:
   ```bash
   git remote set-url origin git@github.com:ericavettorello/FastApi.git
   ```

## 📋 Как это работает

1. Вы делаете изменения в файлах
2. Выполняете: `git add .` и `git commit -m "описание"`
3. Post-commit hook автоматически выполняет `git push`
4. Изменения отправляются на GitHub

## 🔍 Проверка работы

```bash
# Сделайте тестовый коммит
echo "# Test" >> TEST.md
git add TEST.md
git commit -m "Test auto-push"

# Должно автоматически выполниться:
# 🚀 Автоматическая отправка на GitHub...
# ✅ Изменения отправлены на GitHub
```

## ⚠️ Важные моменты

1. **Токен должен быть настроен** - иначе автоматический push не сработает
2. **Проверяйте статус** - если push не удался, выполните вручную
3. **Безопасность** - не коммитьте секреты и токены в код

## 🛠️ Отключение автоматической отправки

```bash
# Удалить hook
rm /root/Autodeploy/.git/hooks/post-commit

# Или переименовать
mv /root/Autodeploy/.git/hooks/post-commit /root/Autodeploy/.git/hooks/post-commit.disabled
```

## 📝 Альтернативные методы

### GitHub Actions (обратная синхронизация)

Можно настроить GitHub Actions для автоматической синхронизации с сервера, но это требует дополнительной настройки webhook.

### Cron job для периодической синхронизации

```bash
# Добавить в crontab
*/5 * * * * cd /root/Autodeploy && git push origin main 2>&1
```

### Мониторинг изменений

Можно создать скрипт, который отслеживает изменения и автоматически коммитит и пушит их.

## ✅ Преимущества

- ✅ Автоматическая синхронизация
- ✅ Не нужно помнить о push
- ✅ Всегда актуальная версия на GitHub
- ✅ Простая настройка

## 🔒 Безопасность

- Используйте Personal Access Token с минимальными правами
- Не храните токены в открытом виде
- Регулярно обновляйте токены
- Используйте SSH ключи для большей безопасности

